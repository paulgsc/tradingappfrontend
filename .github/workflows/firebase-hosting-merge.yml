# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_USER_DATABASE_44E3B }}"
          channelId: live
          projectId: user-database-44e3b
        env:
          VITE_APP_FIRE_BASE_API_KEY: ${{ secrets.VITE_APP_FIRE_BASE_API_KEY }}
          VITE_APP_FIRE_BASE_AUTH_DOMAIN: ${{ secrets.VITE_APP_FIRE_BASE_AUTH_DOMAIN }}
          VITE_APP_FIRE_BASE_PROJECT_ID: ${{ secrets.VITE_APP_FIRE_BASE_PROJECT_ID }}
          VITE_APP_FIRE_BASE_STORAGE_BUCKET: ${{ secrets.VITE_APP_FIRE_BASE_STORAGE_BUCKET }}
          VITE_APP_FIRE_BASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_APP_FIRE_BASE_MESSAGING_SENDER_ID }}
          VITE_APP_FIRE_BASE_APP_ID: ${{ secrets.VITE_APP_FIRE_BASE_APP_ID }}
          VITE_APP_MEASUREMENT_ID: ${{ secrets.VITE_APP_MEASUREMENT_ID }}
          VITE_APP_DEVELOPMENT_URL: "http://localhost:8000"
          VITE_APP_BACKEND_URL: "https://tradingapp.up.railway.app"
